import Logo from "../Logo/Logo";
import NavBar from "../NavBar/NavBar";

const Header = () => {
    return (
        <div>
            <Logo></Logo>
            <NavBar></NavBar>
        </div>
    );
};

export default Header;